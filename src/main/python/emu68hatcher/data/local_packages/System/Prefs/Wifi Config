/* Wifi Config launcher - double-click fires the real script in S:.
 * Absolute path on rx so the inner shell finds it regardless of
 * whether Rexxc: / C:rx are on the user's PATH. */
address command 'SYS:Rexxc/rx s:WifiConfig.rexx'
