; Network Manager launcher - launched via IconX so a CLI exists for 'address command'.
; CLOSE dropped so the window stays open after the script exits.
SYS:Rexxc/rx >CON:0/20/680/400/NetworkManager/AUTO/WAIT s:NetworkManager.rexx
